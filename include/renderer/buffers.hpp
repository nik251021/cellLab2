#pragma once

#include <glad/glad.h>

class vertexBuffer
{
private:

public:
    vertexBuffer();
    ~vertexBuffer();
};

class indexBuffer
{
private:

public:
    indexBuffer();
    ~indexBuffer();
};